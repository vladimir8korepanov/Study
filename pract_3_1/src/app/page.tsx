import { Card } from '@/components/Card';

export default function Home() {
  return (
    <div>
      <Card
        title="Пример карточки"
        content="Это TS версия компонента Card с кнопками внутри"
      />
    </div>
  );
}